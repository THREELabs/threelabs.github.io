import { SafariJeep } from './SafariJeep.js';

/**
 * Re-export SafariJeep as SportsCar for backward compatibility.
 * The player vehicle is now the authentic Old Safari Overland Expedition Jeep.
 */
export const SportsCar = SafariJeep;
export { SafariJeep };
export { SafariJeep as TrophyTruck };
